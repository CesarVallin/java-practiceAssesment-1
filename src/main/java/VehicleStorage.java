public interface VehicleStorage {

    /** Instance method */
    /**
     * An interface instance method does not need the `public` or `abstract` keywords... they are included
     * automatically...
     * */
    String storeStuff(String stuff);
}
